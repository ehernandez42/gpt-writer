# Components

## Frontend

- `Layout` navigation shell
- `StyleForm` create-style form
- `StyleList` renders stored styles
- `FileUploader` file picker
- `GeneratorForm` style selection and prompt form
- `ProviderBadge` shows active provider label
- `GenerationEditor` editable output with export actions
- `ErrorBoundary` render fallback on UI crashes
